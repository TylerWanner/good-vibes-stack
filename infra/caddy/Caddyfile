# Caddyfile — reverse proxy for nervous-system-api
# Set DOMAIN in .env (e.g. DOMAIN=api-gvs.reptilian.dev)
# Caddy auto-provisions HTTPS via Let's Encrypt.

{$DOMAIN:localhost} {
    reverse_proxy nervous-system-api:8001
}
